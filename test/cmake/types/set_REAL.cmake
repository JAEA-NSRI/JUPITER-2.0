if(JUPITER_FP_PRECISION STREQUAL DOUBLE)
  if(JUPITER_INPUT_TEST_BINARY_OUTPUT_DOUBLE)
    set(_T0N DOUBLE)
  else()
    set(_T0N SINGLE)
  endif()
  if(JUPITER_INPUT_TEST_RESTART_BINARY_OUTPUT_DOUBLE)
    set(_T1N DOUBLE)
  else()
    set(_T1N SINGLE)
  endif()
  if(JUPITER_INPUT_TEST_RESTART_OUTPUT_DOUBLE)
    set(_T0R DOUBLE)
  else()
    set(_T0R SINGLE)
  endif()
  if(JUPITER_INPUT_TEST_RESTART_RESTART_OUTPUT_DOUBLE)
    set(_T1R DOUBLE)
  else()
    set(_T1R SINGLE)
  endif()
  set(_T0M DOUBLE)
  set(_T1M DOUBLE)
else()
  set(_T0N SINGLE)
  set(_T0R SINGLE)
  set(_T1N SINGLE)
  set(_T1R SINGLE)
  set(_T0M SINGLE)
  set(_T1M SINGLE)
endif()
